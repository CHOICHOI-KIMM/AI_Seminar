// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Alert } from "./Alert";
import type { BallBearingKind } from "./BallBearingKind";
import type { BbEquilibrium } from "./BbEquilibrium";
import type { BbGeometrySummary } from "./BbGeometrySummary";
import type { BbPhaseSweepResult } from "./BbPhaseSweepResult";

/**
 * 정상상태 해석 결과 최상위.
 *
 * 수명(P4)·윤활(P5) 결과는 해당 Phase 에서 필드를 추가한다 (P1-S2 결정).
 */
export type BbResult = { 
/**
 * 입력의 볼베어링 변종을 그대로 반영한 **판별자** (§3.6.1.7).
 *
 * 통합 시 여러 계열의 결과가 한 채널로 흐른다. 판별자가 없으면 serde 가
 * 필드가 겹치는 다른 계열 결과로 **조용히 역직렬화**한다 — 그것을 막는 장치다.
 * `BbInput::kind` 와 항상 같아야 한다 (A-8c 가 필드 존재를, 런타임
 * 계약검사가 값 일치를 검사한다).
 */
kind: BallBearingKind, geometry: BbGeometrySummary, equilibrium: BbEquilibrium, phase_sweep: BbPhaseSweepResult | null, alerts: Array<Alert>, elapsed_ms: number, };
