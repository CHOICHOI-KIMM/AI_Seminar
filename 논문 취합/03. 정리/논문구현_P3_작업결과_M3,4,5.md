# 논문구현 P3 작업결과 — 표면하 응력·피로·마모 (M3+M4+M5) + 시간루프

> **신규(2026-07-14)**: M3/M4/M5 착수를 위한 결과 로그. M1/M2/M6 부분윤활 서브시스템 결과는 `논문구현_P3_작업결과_M1,2,6.md`(리네임 전 `논문구현_P3_작업결과.md`). 계획·인터페이스 검토 정본은 `논문구현_P3_총괄계획.md` **§8.C**. 방법론은 §8.A/§8.B 검증패턴(독립오라클·변이게이트·적대 재크리틱·근거기반) 계승.
> **현 상태**: 착수 전 **종합 검토 완료**(아래). 코드 미착수. 기준선 = M1+M2+M6 G3 통과(독립 `cargo test` **58 단위 + 2 통합 green**, 브랜치 `phase2-partial-lub`, HEAD `45353a4`/코드 `085b06d`).

---

## 0. 착수 전 종합 검토 (2026-07-14)

M3/M4/M5 착수에 앞서 P2 정본(P2-1 이론·P2-2 검증·P2-3 알고리즘) + 참고문헌 정본에서 식·오라클·규약·RQ 를 verbatim 소급하고, Phase 2 출력과의 인터페이스를 검토했다. 상세 계획은 총괄계획 §8.C. 요지:

### 0.1 데이터 흐름 (DAG (M1,M2)→M6→M3→(M4,M5)→시간루프)

| 단계 | 입력(출처·단위) | 산출 | M3 의존 |
|---|---|---|---|
| **M3 응력** | `p_tran`[Pa]·`q_tran=μ·p_tran`[Pa] (Phase2) | σ_ij(x,y,z) 6성분 [Pa], z=0~0.25a·15층 | — |
| **M4 피로** | σ_ij(x,y,z,**t**) 6성분 **시간이력** [M3] | Dang Van D·수명 N·손상맵 d, d>1→피트 | **필요** |
| **M5 마모** | `p_tran`[Pa]·`u_s`[m/s]·`phi_bl`[-] (Phase2) | 마모깊이 Δh_w [m/cycle] | **불요** |
| **시간루프** | d(M4)·Δh_w(M5) | 형상·거칠기 갱신 → Fig10/11 | — |

Phase 2 `solve_partial` 출력 `PartialLubResult{p_tran,h_tran,phi_bl,q_tran}` 이 하류 계약과 정합(확인). `q_tran=μ_eff·p_tran`, `μ_eff=φ_bl·μ_bl+(1−φ_bl)·μ_ehl` (`partial_lub.rs` 확정).

### 0.2 핵심 설계판단·확장 필요 (착수 전 확정)

1. **types.rs(동결 SSOT) 확장** — 신규 산출물 타입(`StressResult`·`FatigueResult`·`WearResult`)·깊이층 필드(z층별 6성분)는 **신규 struct 추가**라 기존 `Field2`/`Grid`/리터럴 무영향(파급 최소). serde derive 추가.
2. **접촉반폭 a/b 입력(★공유 enabler)** — M3 깊이 `z=0~0.25a`·Hertz 오라클 `σ_vM/p_H=0.557@z/b=0.73`·M5 `u_s→사이클당 거리=(접촉폭/속도)` **모두 접촉반폭 필요**. → `OperatingConditions` 에 **`r_x`(등가반경) 1필드 추가**, `b=4R·p_h/E'=2R·p_h/E_red`(Hertz 선접촉) 유도. 파급: 리터럴 ~5곳(dummy_op·mixed_input) `r_x` 추가 + serde. **부수이득**: 동일 `r_x` 가 P4 에서 RQ-M2-comp-curve(∇=λ/b) 결선까지 해금.
3. **시간(t)=이동하중 x-스냅샷** — M4 시간이력은 정상상태 단일형상에서 **M3 한 공간스냅샷의 x-축 변화**(T=x/ū)로 구성 → 전 시간루프 선행 없이 M4 착수 가능. 전 형상진화는 P5 여지.
4. **부호·규약 인계** — M3 z>0 내부·인장(+)·`e^{−ζz}` → M4 `p̂=⅓trσ` 부호 명시 인계. α 삼중분리(alpha_wave/alpha_dv≈0.232/alpha_visc) 혼용 금지. SSOT 전략 B 유지.

### 0.3 모델별 근거·리스크 요약

| 모델 | 정본 식 | 핵심 독립오라클(반-tautology) | 잔여 RQ | 리스크 |
|---|---|---|---|---|
| **M3** | Tripp 2003 **식[10](법선)/[16](트랙션)** 6응력 폐형식 (2011 식[12][13] OCR손상 σ_y **사용금지**) | **VC-M3-Trace** `σ_x+σ_y+σ_z=2(1+ν)p₀e^{−ζz}coscos`(정본판별)·VC-M3-Sin·Hertz 0.557@0.73·RP-Depth 3~4µm | RQ-09/10 결과무해 해소·ζ 정의 표준재구성. **미결 0건** | **낮음** |
| **M4** | 식[9] `D=maxₜ{τ̂/(τ_f−α_dv·p̂)}`+Wöhler `σ_f=A·lnN+B`(**A=−43.0·B=1220**)+Miner d>1. τ̂/p̂ = **Desimone 식(1)~(7) verbatim** | **VC-M4-Milano/MesoA**(Milano 닫힌해 ≤1%)·Desi Fig.3·Wohler | **G-M4-1 실질해소**(§0.5): Desimone 식(1)~(7) 원문소급 완료·잔류전단 불필요·(34)/(36) 불요. N_ref=1e6 = SKF 가정(Desimone 아님) | **중저**(MCE 수치구현 1건) |
| **M5** | Archard **식[14] `Δh_w/Δn=k·p·u_s/H`**·식[15]; k_dry=f_w·k_lub | **VC-M5-Archard** `W=KP/3a`(Archard1953 verbatim)·CV-M5-Dim·RP-Λmax/Slip 정성 | **G-M5-1** k_lub 가정+민감도(1e-11~5e-10)·하한 외삽태깅. 피로↔마모 경쟁 정량식 미제공(ref(10) 미보유)→UPD 창발구조 | **중저** |

**핵심 리스크 Top5**: ① M4 MCE(편차경로 최소외접초구, Desimone 식5) 수치구현 → Milano 닫힌해 독립검증. ② M4 시간이력=x-스냅샷 해석 오적용 주의. ③ k_lub 1.7-decade 가정(민감도 필수). ④ `r_x` 동결계약 확장(파급 리터럴 갱신·58+2 회귀). ⑤ M3 2011 식 σ_y 사용금지.

### 0.4 착수 순서 권장

**M3 먼저**(M4 의존 해소 + 응력 인프라·`r_x`/접촉반폭 처리 확립, 근거완비·저리스크) → **M4·M5 병렬**(M5 는 M3 비의존이라 조기 병렬 여지) → **시간루프**(단일스텝 G4 우선, 전 재현 P5 여지).

### 0.5 ref(33) Desimone 2006 재조사 — G-M4-1(Dang Van τ̂/p̂ 계산법) 실질 해소 (2026-07-14)

원 SKF 2011 논문이 Dang Van 응력이력 τ̂/p̂ 계산법을 "will not be discussed here"로 부재 처리해 **G-M4-1**(M4 최대 난점)로 남았으나, `2006. (Desimone) On the application of Dang Van criterion to rolling cont_58b30a.md`(repo 보유) 원문 직접 정독 결과 **실무 계산법 전체가 식(1)~(7)로 명시**됨을 확인 → gap 실질 해소. verbatim:

- **식(1)** `τ_max(t) + a_DV·σ_H(t) = τ_W` — 파손 locus (τ_W=역전 비틀림 피로한계).
- **식(2)** `τ_max(t) = ½(ŝ_I(t) − ŝ_III(t))` — **대칭화 편차응력의 Tresca**(주값 ŝ_I≥ŝ_II≥ŝ_III).
- **식(3)** `s_ij(t) = σ_ij(t) − δ_ij·σ_H(t)`, `σ_H(t) = ⅓ tr σ(t)` — 편차·정수압.
- **식(4)** `ŝ_ij(t) = s_ij(t) − s_ij,m` — 대칭화(상수텐서 감산).
- **식(5) [MCE 정의]** `s_ij,m` 은 `maxₜ[(s(t)−s_m)·(s(t)−s_m)] = min_{s'} maxₜ[(s(t)−s')·(s(t)−s')]` 를 만족 — **편차응력경로 {s_ij(t)} 의 최소외접초구 중심**(Chebyshev center). 피로 크랙핵생성에 무영향인 편차분을 최소화로 제거.
- **식(7)** `a_DV = 3(τ_W/σ_W − ½)` — τ_W/σ_W=1/√3(von Mises) → **a_DV≈0.232** 확정. Desimone 경고: 강재는 τ_W/σ_W>0.5 가능 → 민감도.
- **식(6)** 잔류 시 `τ_max(t) + a_DV[σ_H(t) + σ_H,res] = τ_W` — 잔류는 **정수압에만** 가산.

**구현 함의(재조사 결론)**:
1. **G-M4-1 해소**: τ̂/p̂ 계산법 완전 확보(더는 gap 아님). MCE(식5)만 수치 부문제로 남음 — Welzl 최소외접구/볼록 QP(잘 알려진 계산기하). 시간이력=x-스냅샷.
2. **잔류전단응력 불필요**(L109·L134 명시): 대칭화(식2/4)로 잔류전단 자동소거, 잔류는 σ_H만 평행이동. → M4 구현 대폭 간소화(잔류 모델 불요, 무잔류 가정도 τ̂ 불변).
3. **(34)Bernasconi/(36)Baietto 불요**: 식(5)가 τ̂ 계산법을 완전 명시 → 미보유 두 편 없이 충분(참고문헌 "필요"→불요로 정정).
4. **정직 교정**: 앞선 M4 추출이 "(33)이 N_ref 제공"이라 했으나 **오류** — Desimone 는 **피로한계(무한수명 locus) 논문**으로 N_ref(유한수명) 계산이 없음. N_ref=1e6 은 SKF 2011 가정+민감도로 유지(Desimone 소급 아님).
5. **모델선택 유의**: Desimone 의 논문 기여는 **2-slope locus**(단일선 a_DV 가 RCF 압축역서 과대예측이라 비판, 수평구간 a_DV=0 + 음기울기). 그러나 **우리 M4 는 SKF 2011 단일선 a_DV≈0.232 채택**(정본). VC-M4-Desi 오라클(Fig.3a: 구반경1mm·μ=0.1·p_o/k=3.5)은 "**원 단일선 Dang Van 이 파손예측 안 함**" 재현으로 해석(Desimone 2-slope 는 파손예측 — 우리 채택 아님). Table1(R=−2/−1/0.1/0.3 → 1/1/0.76/0.68)=VC-M4-DesiTab 그대로.
6. **오라클 원천 확인**: Milano2006(`2006. (Milano) ..._37cfdd.md`)·Desimone2006 **둘 다 repo 보유** → VC-M4-Milano(닫힌해 ≤1%)·VC-M4-Desi(Fig.3) 정본 대조 가능. 응력계산은 Desimone 예제서 Sackfield-Hills[20](구접촉)이나 우리 검증은 Milano 닫힌해가 주 앵커(M3 Tripp 과 독립).

→ **M4 리스크 중 → 중저 하향**. 남은 실질 난점 = MCE(식5) 수치구현 + 시간이력 해석 정합뿐.

---

## M3 — 표면하 응력 (Tripp 2003) — **구현 완료·G3 통과 (2026-07-14, 무인 wf_7cc32e63)**

### 1. 구현 (src/m3_stress.rs 신규, 917줄)
- **정본**: Tripp 2003 식[10](법선 p₀→6응력)/식[16](접선 q₀→6응력) 폐형식 verbatim. **2011 식[13] σ_y 손상식 미사용**(원전만; `vc_m3_trace_*`가 trace 항등으로 정본 확증).
- **알고리즘**: 실 하중장 p_tran·q_tran 각 1회 2D-FFT → bin별 복소 FRF(식[10]/[16]) 선형적용 → 6성분 IFFT **중첩**(convolution 아님). 공통 감쇠 `e^{−ζz}`, ζ=√(α²+β²). Hermitian `H(−a,−b)=conj(H(a,b))`로 IFFT 실수성. 짝수격자 Nyquist 홀수패리티 마스킹(m2_lub 일반화).
- **인터페이스**(types.rs 동결 준수): `solve_stress(&StressInput)->StressResult`(z=0~0.25b·15층, b=2·r_x·p_h/e_red) + `solve_stress_at_depths(...)` 코어(오라클·M4 재사용) + `von_mises(...)`·`contact_half_width(...)`.
- **Phase0 선행**(오케스트레이터): `OperatingConditions.r_x` + `StressTensor6`/`StressResult`(types.rs), 리터럴 5곳(커밋 `bb9c58e`).

### 2. 오라클 11개 (독립·비-tautology)
- **VC-M3-Sin**(법선/접선): 단일 정현파 → 식[10]/[16] **손유도 닫힌형**과 ≤1e-6 일치(솔버 복소FRF 경로와 독립).
- **VC-M3-Trace**(법선/접선): `σx+σy+σz=2(1+ν)p₀e^{−ζz}coscos` 정본판별(2011 손상식 불성립).
- **VC-M3-Limit**: β=0→1D 식[4], τ_xy·τ_yz→0. **경계(z=0)**: σ_zz=p·τ_xz=0(법선), τ_xz=q·σ_zz=0(접선).
- **VC-M3-Hertz**(외부앵커): 매끈 Hertz 선접촉 → vM 최대. 독립기준=McEwen 폐형식(고전 0.5575@z/b≈0.70; 원문 아님).
- **RP-Depth**: λ=40µm 리플 → 최대 vM 깊이 ≈λ/2π≈6.4µm(차수 확인).
- 보조: DC 균일하중·형상/깊이·von Mises 표준.

### 3. 무인 게이트 결과
- **변이게이트 3/3 CAUGHT**(감쇠부호 e^{−ζz}→e^{+ζz}·트레이스계수 2(1+ν)→(1+ν)·감쇠제거 e^{−ζz}→1) → 관련 오라클 FAIL 확인·정확 원복. MISSED 없음.
- **적대 3-크리틱 전부 pass·`fabrication_found=false`**(tautology/grounding/fabrication): 식·계수 전부 Tripp 2003 소급, 자유계수 없음, 오라클 자기참조 없음.
- **human-gate 1건 → 오케스트레이터 2단 조치**: fabrication 렌즈가 VC-M3-Hertz가 실피크 0.5973(+7.1%)를 8% 밴드로 겨우 통과함을 지적. **근원=periodic-window 평균(DC) 아티팩트**. (a) DC 측방항 0→ν/(1−ν)σ_zz 교정(+7.1%→+3.4%), (b) 대형창 수렴(256b, +0.49%) → **오차 <1% 결선, tol 8%→1%**. 상세는 아래 §3b.

### 3b. VC-M3-Hertz "실피크"의 정확한 의미 + <1% 결선 (2026-07-14 보강)

**"실피크"란 무엇인가**: VC-M3-Hertz 오라클은 매끈 Hertz 선접촉 압력 `p(x)=p_h√(1−(x/b)²)`(|x|<b)를 **M3 솔버**(2D-FFT→식[10]/[16] FRF→IFFT)에 입력해 얻은 **표면하 von Mises 응력의 축상(on-axis) 최대값**이다. 이를 두 값과 대조:
- **고전 기준(McEwen)**: 고립(비주기)·무한 반공간 Hertz 선접촉의 축상 최대 vM = **0.5575·p_h @ z≈0.70b**(교과서 폐형식, 원문 아님·외부 독립앵커).
- **솔버 실피크**: M3가 실제 산출한 값. 즉 "솔버가 매끈 Hertz에 대해 내놓는 표면하 vM 최대가 고전값을 얼마나 재현하는가"의 척도(구현 정확도 측정).

**왜 편향이 났나 (물리적 원천)**: 솔버는 **주기적 FFT**(유한창)로 계산하나 McEwen은 **고립·무한** 해다. 두 원천:
1. **평균압(DC) 아티팩트 [지배]**: Hertz 범프는 창 평균이 0이 아님(p̄≈π/64·p_h≈0.049p_h). 주기 프레임에선 이 평균이 **전 표면 균일하중**으로 취급돼 전 깊이에 구속 응력을 더한다 — **고립 Hertz엔 없는 성분**. p̄∝1/창.
2. **이미지 하중·이산화**: 이웃 주기 범프(±창 간격)·격자 유한성 기여(작음).

**<1% 결선 (2단계, 전부 근거·실측)**:
- (a) **DC 측방항 교정**: `σ_xx=σ_yy=0` → `ν/(1−ν)σ_zz`(구속 균일하중 고전 참응답, Hooke `ε_lat=0`서 유도) → +7.1%→+3.4%(32b창).
- (b) **대형창 수렴**: 오차∝1/창 **실측**(32b→+3.4%, 64b→+1.80%, 128b→+0.93%, 256b→+0.49%). **dx를 b/32→b/64로 세분해도 불변**(0.93%→0.91%) → 편향은 **순수 창(DC) 효과**이지 이산화가 아님을 확증. **256b·nx8192 채택 → +0.49%(<1%)**. 피크 깊이 z/b=0.699(고전 0.70과 정합).

**정합의 의미**: **깊이(0.70b)는 처음부터 정확**했고 편향은 오직 피크 크기(주기-평균 아티팩트)에 있었다. 대형창 수렴은 "주기 FFT로 비주기 접촉을 검증"하는 표준기법(zero-pad→창→∞)이라 **정당한 수렴이며 masking 아님**. 대안=평균분리(P2-3 §3.3, 작은 창서 DC를 해석적으로 제거)이나 대형창이 등가·단순. **실사용(거칠기 리플)엔 평균압이 물리적**이라 DC 구속응답이 옳고 대형창은 **검증 셋업 한정**(솔버 불변). 잔여 RQ-M3-DC = 접선(q) DC 완전형만.

### 4. 검증·잔여
- **독립 cargo test**: 69 단위 + 2 통합 green(58 기준선 + 11 신규 M3). types.rs 불변, 변이잔재 없음, 경고 없음. 커밋 `8342e2d`.
- **VC-M3-Hertz <1% 결선(§3b)**: DC 측방항 교정 + 대형창(256b)으로 **+0.49%** 달성(tol 1%). 오라클 비용은 y-불변 활용(ny=4)·피크대역 집중(nz=31)으로 전체 스위트 3.7s 유지. **잔여 RQ-M3-DC = 접선(q) DC 완전형만**(법선·평균 아티팩트는 해소).
- **크리틱 비차단 지적**(기록): VC-M3-Trace는 VC-M3-Sin 성분합이라 독립 검출력 종속(정본판별 목적은 유효); Nyquist 마스킹 직접구동 오라클 부재(광대역 간접검증만); dc_uniform_pressure는 접선 DC(q) 미검증. → 폴리시 여지(비필수).

### 5. 다음
- **M4 착수**(§8.C.3): M3 σ_ij(x,y,z) 를 시간이력(x-스냅샷)으로 소비 → Dang Van(Desimone 식1~7·MCE). `solve_stress_at_depths` 재사용.

## M4 — 다축피로 (Dang Van/Milano)

*(착수 대기 — 계획 §8.C.3)*

## M5 — 경마모 (수정 Archard)

*(착수 대기 — 계획 §8.C.4)*

## 시간루프 (형상갱신)

*(착수 대기 — 계획 §8.C.5)*

---

**끝**
