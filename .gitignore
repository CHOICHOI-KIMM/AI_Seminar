# GitHub 파일당 100MB 제한 초과로 push가 거부되는 대용량 PDF (git 추적 제외, 로컬 보관)
논문 취합/03. 정리/2025. (SKF) Rolling bearing_tribology damage modes and life modelling .pdf

# MinerU 중간 산출물(전체 논문명이 반복되는 깊은 경로 + 대용량 origin.pdf 사본) — 커밋 제외
.mineru_out/
**/.mineru_out/

# P3 micropitting-wasm 빌드 출력 (target/·pkg-node/) — P3 워크스트림에서 재생성 가능, 커밋 제외
논문 취합/03. 정리/논문구현_P3/micropitting-wasm/

# MASTA 세션 잠금 파일 — 데이터 아님
Main bearing/02. 자료/MASTA/*.lock

# MS Office 임시 잠금 파일
**/~$*

# Python 캐시
__pycache__/
*.pyc

# 파이프라인 실행 로그 (재생성 가능)
Main bearing/01. 정리/Python 자동화/Results/DLC별해석/*.log

# Claude 환경 export zip (비밀정보·세션 포함 — 커밋 금지)
**/claude_env_export_*.zip

# Claude 환경 추출 폴더 (.env·세션 포함 — 커밋 금지)
**/claude_env_export_*/

# 부록5 검증용 MASTA 파일 (사용자 지시로 커밋 제외, 260727)
Main bearing/02. 자료/MASTA/*_검증_*.Masta

# 해석 실행 노이즈 (260728)
*.err
~$*
Main bearing/01. 정리/Python 자동화/Results/**/*.log

# MASTA 모델 파일 — 스크립트로 재생성 가능한 대용량 바이너리(파일당 8MB+), 커밋 제외
# 260819: 실제 확장자가 .Masta(대문자 M)라 *.masta 규칙이 한 번도 매칭되지 않았음
**/MASTA/*.[Mm]asta
**/MASTA/*.lock

# 스크리닝 ε 맵 — 설계 × DLC × k 301수준이라 파일당 60 MB 다 (260805)
# 진단용 중간 산출이고 screen_k.csv 로 최종 k 가 남으므로 추적하지 않는다
**/screen_eps_map.csv

# Obsidian 편집기 레이아웃 — 볼트를 열 때마다 갱신되어 매번 변경으로 잡힌다 (260819)
.obsidian/workspace.json

# TRB-main: 프론트엔드 구조 참조용 로컬 사본 (원본은 별도 관리, 커밋 제외 260821)
TRB-main/
